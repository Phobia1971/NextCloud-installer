# NextCloud-installer
A single, interactive Bash script that turns a fresh Ubuntu VM into a running **Nextcloud All-in-One** server. No Docker Compose files to edit, no RAID commands to memorize — just run it and answer a few questions.
